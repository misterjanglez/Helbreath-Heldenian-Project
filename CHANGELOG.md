# Scrollable Graphics tab in System Menu

Added scrolling support to the Graphics tab in the System Menu so content can overflow the visible area. Includes a custom scrollbar with track and scrub sprites, and mouse wheel scrolling when hovering the content area.
